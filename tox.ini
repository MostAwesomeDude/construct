[tox]
envlist=
    test

[testenv]
basepython=
    python3.6

deps=
    pytest
    pytest-cov
    pytest-benchmark
    numpy

[testenv:test]
commands=
    py.test --benchmark-skip {posargs}

[testenv:testverb]
commands=
    py.test --verbose --fulltrace --showlocals --benchmark-skip {posargs}

[testenv:cover]
commands=
    py.test --cov {envsitepackagesdir}/construct --cov-report html --cov-report term --benchmark-skip --verbose {posargs}

[testenv:bench]
commands=
    py.test --benchmark-only --benchmark-columns=min,max,mean,stddev,median,rounds --benchmark-compare {posargs}

[testenv:benchsave]
commands=
    py.test --benchmark-only --benchmark-columns=min,max,mean,stddev,median,rounds --benchmark-autosave {posargs}
